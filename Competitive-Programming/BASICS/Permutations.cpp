#include<bits/stdc++.h>
using namespace std;

int n;
void arrange(vector<int> arr){
    if()
}

int main(){
    cin >> n;
    vector<int> arr(n);
    for(int i=0;i<n;i++) arr[i] = i+1;
    arrange(arr);
    vector<int> res(n);
    
}